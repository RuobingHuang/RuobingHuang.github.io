// Assuming the original content looks like this:
const publicationHtml = `
  <div>
    <h1>Publications</h1>
    ${yearSectionsHtml || '<p>No publications available.</p>'}
  </div>`;

// Updated code
const yearSectionsHtml = ''; // Define your variable here based on the logic you need
const publicationHtml = `
  <div>
    <h1>Publications</h1>
    ${yearSectionsHtml || '<p>No publications available.</p>'}
  </div>`;
